---
title: 本地开发
description: 前端优先的本地开发方式
---

# 本地开发

当前主应用以 `web/` 前端为主，AI 模型请求由浏览器调用同源 `/api/new-api/gateway`，再由 Next.js 服务端代理到 `api.antsk.cn`。

## 1. 启动前端

```bash
cd web
bun install
bun run dev
```

前端默认访问：

```text
http://localhost:3000
```

## 2. 配置模型

打开右上角配置弹窗，填写自己的 `API Key` 和模型名；`Base URL` 固定为同源 new-api 代理。第三方提示词由 Next.js route 拉取并缓存在运行实例内存中；WebDAV 可选择前端直连或 Next.js 转发。

## 3. 启动文档站

如果需要单独调整文档站，在 `docs` 目录执行：

```bash
bun run dev
```

## 常见场景

- 改画布、页面和交互：主要看 `web/`
- 改提示词缓存或 WebDAV 代理：主要看 `web/src/app/api/` 和 `web/src/app/webdav-proxy/`
- 改文档站内容：主要看 `docs/content/docs/`
