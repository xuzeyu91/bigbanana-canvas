services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: bigbanana-canvas:local
    container_name: bigbanana-canvas
    ports:
      - "3006:3000"
    restart: unless-stopped
